# Replace overlays

Place files here that should overwrite matching paths inside the Open WebUI container.
The `scripts/preflight.sh` helper copies this directory into the container root so its
structure should mirror the destination filesystem.
